apiVersion: v1
kind: Secret
metadata:
  name: cloudflare-dns
  namespace: networking
type: Opaque
# Create this secret manually with your Cloudflare API token:
# kubectl create secret generic cloudflare-dns \
#   --namespace networking \
#   --from-literal=api-token='your-cloudflare-api-token-here' \
#   --dry-run=client -o yaml | kubectl apply -f -
