apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: copyparty-data
  namespace: copyparty
  labels:
    app: copyparty
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: zfs-localpv
  resources:
    requests:
      storage: 10Gi
