apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: pocket-id
  namespace: security
  annotations:
    external-dns.alpha.kubernetes.io/cloudflare-proxied: "true"
spec:
  endpoints:
    - dnsName: pid.saleem.us
      recordType: CNAME
      targets:
        - external.saleem.us
