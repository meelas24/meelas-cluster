apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: memos
  namespace: cloud
  annotations:
    external-dns.alpha.kubernetes.io/cloudflare-proxied: "true"
spec:
  endpoints:
    - dnsName: memos.saleem.us
      recordType: CNAME
      targets:
        - external.saleem.us
