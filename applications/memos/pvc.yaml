apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: memos-data
  namespace: cloud
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: zfs-localpv
  volumeMode: Filesystem
