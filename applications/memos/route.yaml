apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: memos
  namespace: cloud
spec:
  parentRefs:
    - name: gateway-external
      namespace: network
      sectionName: https
  hostnames:
    - memos.saleem.us
  rules:
    - backendRefs:
        - name: memos
          port: 5230
          weight: 1
